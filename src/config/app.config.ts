/**
 * Application-wide configuration constants for KUMSS ERP
 */

/**
 * Default page size for standard list/table API calls.
 */
export const DEFAULT_PAGE_SIZE = 10;

/**
 * Page size for dropdown/select components that need to load
 * all available options (e.g., searchable dropdowns, form selects).
 */
export const DROPDOWN_PAGE_SIZE = 50;
